import { Options } from 'react-markdown';
type MarkdownProps = Options & {
    children?: string;
};
export declare const Markdown: import("@chakra-ui/system/dist/system.types").ChakraComponent<({ children, ...rest }: MarkdownProps) => JSX.Element | null, {}>;
export {};
