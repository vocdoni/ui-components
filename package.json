{
  "name": "@vocdoni/ui",
  "version": "0.0.1",
  "description": "Vocdoni UI components and related packages",
  "main": "index.js",
  "license": "GPL-3.0-or-later",
  "private": true,
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "devDependencies": {
    "prettier": "^2.8.7",
    "turbo": "^1.8.6"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint:fix": "prettier -c . --write",
    "lint": "prettier -c .",
    "prepare": "yarn workspace dotobject build"
  },
  "packageManager": "yarn@1.22.19"
}
